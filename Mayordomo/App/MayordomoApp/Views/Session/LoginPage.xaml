<?xml version="1.0" encoding="UTF-8"?>
<ContentPage
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="MayordomoApp.Views.Session.LoginPage"
    NavigationPage.HasNavigationBar="False"
    xmlns:ios="clr-namespace:Xamarin.Forms.PlatformConfiguration.iOSSpecific;assembly=Xamarin.Forms.Core"
    xmlns:controls="clr-namespace:MayordomoApp.Controls"
    xmlns:fontawesome="clr-namespace:MayordomoApp.Fonts"
    ios:Page.UseSafeArea="True"
    BackgroundColor="White">
    <ContentPage.Content>
        <ScrollView
            VerticalScrollBarVisibility="Never">
            <StackLayout>
                <StackLayout
                    Spacing="15"
                    Padding="30,0"
                    VerticalOptions="CenterAndExpand">
                    <Image
                        HeightRequest="150"
                        Margin="20,0,20,40"
                        Source="MAYORDOMO"/>
                    <controls:CanvasView
                        CornerRadius="4"
                        BackgroundColor="#EDEDED">
                        <controls:CanvasView.Padding>
                            <OnPlatform x:TypeArguments="Thickness">
                                <On Platform="Android" Value="10,0"/>
                                <On Platform="iOS" Value="10,15"/>
                            </OnPlatform>
                        </controls:CanvasView.Padding>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Label
                                TextColor="#1C2E29"
                                VerticalOptions="Center"
                                Grid.Column="0"
                                Text="{x:Static fontawesome:FontAwesome.User}"
                                FontFamily="{StaticResource FontAwesomeSolid}"/>
                            <Entry
                                Text="{Binding User}"
                                Grid.Column="1"
                                TextColor="#1C2E29"
                                Placeholder="Usuario"/>
                        </Grid>
                    </controls:CanvasView>
                    <controls:CanvasView
                        CornerRadius="4"
                        BackgroundColor="#EDEDED">
                        <controls:CanvasView.Padding>
                            <OnPlatform x:TypeArguments="Thickness">
                                <On Platform="Android" Value="10,0"/>
                                <On Platform="iOS" Value="10,15"/>
                            </OnPlatform>
                        </controls:CanvasView.Padding>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Label
                                VerticalOptions="Center"
                                TextColor="#1C2E29"
                                Grid.Column="0"
                                Text="{x:Static fontawesome:FontAwesome.Password}"
                                FontFamily="{StaticResource FontAwesomeSolid}"/>
                            <Entry
                                Text="{Binding Password}"
                                Grid.Column="1"
                                IsPassword="True"
                                TextColor="#1C2E29"
                                Placeholder="Contraseña"/>
                        </Grid>
                        
                    </controls:CanvasView>
                    <Button
                        CornerRadius="0"
                        BackgroundColor="#1C2E29"
                        Text="Iniciar Sesion"
                        FontSize="Body"
                        Command="{Binding SigInCommand}"
                        ContentLayout="Right,10"
                        TextColor="White">
                        <Button.ImageSource>
                            <FontImageSource
                                FontFamily="{StaticResource FontAwesomeRegular}"
                                Glyph="{x:Static fontawesome:FontAwesome.Send}"
                                Size="20"
                                Color="White" >
                            </FontImageSource>
                        </Button.ImageSource>
                    </Button>
                    <StackLayout
                        Margin="0,0,30,0"
                        Grid.Row="0"
                        HorizontalOptions="Center"
                        Orientation="Horizontal">
                        <Label
                            TextColor="#1C2E29"
                            Text="¿Nuevo?"/>
                        <Label
                            FontAttributes="Bold"
                            TextColor="#1C2E29"
                            Text="Registrate Aqui!!">
                            <Label.GestureRecognizers>
                                <TapGestureRecognizer Command="{Binding RegisterCommand}"/>
                            </Label.GestureRecognizers>
                        </Label>
                    </StackLayout>
                </StackLayout>
            </StackLayout>
        </ScrollView>
    </ContentPage.Content>
</ContentPage>
